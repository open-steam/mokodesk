LarsPackagePanel = function(config) {

    LarsPackagePanel.superclass.constructor.call(this, config);
	
	this.addEvents({schuelerEditTab: true});
};
        
Ext.extend(LarsPackagePanel, Ext.Panel, {

});
